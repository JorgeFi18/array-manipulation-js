/**
 *
 * @param {Object[]} products
 * Use find to return the first product whose price is greater than 100
 * @returns {Object}
 */
function findExpensiveProduct(products) {
  return products;
}

module.exports = findExpensiveProduct;
