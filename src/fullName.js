/**
 *
 * @param {people[]} numbs
 * Use a map to create a new property fullName on each person
 * @returns {people[]}
 */
function createFullNames(people) {
  return people;
}

module.exports = createFullNames;