/**
 *
 * @param {Object[]} peopleList
 * Use map to add a new property isAdult that is true if age is >= 18
 * @returns {Object[]}
 */
function addIsAdultProperty(peopleList) {
  return peopleList;
}

module.exports = addIsAdultProperty;
