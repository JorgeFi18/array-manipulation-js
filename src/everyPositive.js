/**
 *
 * @param {number[]} numbers
 * Use every to check if all numbers are positive
 * @returns {boolean}
 */
function everyPositive(numbers) {
  return undefined;
}

module.exports = everyPositive;