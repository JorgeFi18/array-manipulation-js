/**
 *
 * @param {people[]} numbs
 * Use forEach to calculate the total of people above the 20 years old
 * @returns {number}
 */
function olderPeopleCount(peopleList) {
  return 0;
}

module.exports = olderPeopleCount;