/**
 *
 * @param {number[]} numbers
 * Use reduce to calculate the average of the numbers
 * @returns {number}
 */
function calculateAverage(numbers) {
  return 0;
}

module.exports = calculateAverage;
